target/

# Game data is not bundled. Anchor these to the repository root so they do not
# accidentally ignore source directories such as `src/data/`.
/data/
/WOLF3D/
*.WL6
*.WL1

# User configuration (machine-specific paths); `.example` is tracked.
wolf3d-bevy.toml

# Flatpak build output
build-dir/
.flatpak-builder/
